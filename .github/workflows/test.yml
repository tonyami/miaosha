name: Test CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install dependenceies
      run: |
        apt-get update
        apt install iputils-ping

    - name: Ping
      run: ping ccr.ccs.tencentyun.com -c 5
